# Kaplan-Meier UMAP paper

The publication PDF is `umap-of-evidence-stats.pdf`.

Compile from this directory:

```sh
pdflatex umap-of-evidence-stats.tex
bibtex umap-of-evidence-stats
pdflatex umap-of-evidence-stats.tex
pdflatex umap-of-evidence-stats.tex
```

The manuscript is a methods protocol. Numerical UMAP results, author metadata,
and declarations must be completed and human-verified before submission.
